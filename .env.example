# Canonical site URL used for metadata, canonical links, sitemap.xml and robots.txt.
# Defaults to https://semitool.vercel.app when unset. Set this to the production
# host (custom domain) so search engines index a single origin.
NEXT_PUBLIC_SITE_URL=https://semitool.vercel.app
