/**
 * Typed custom-event names for shell-wide orchestration. The traveler modal
 * mounts inside tool pages (ToolPageActions), so the app shell opens it via
 * this event instead of querying the DOM for translated button titles.
 */
export const TRAVELER_OPEN_EVENT = 'semitools:open-traveler';
