export type DateFormat = 'YYMMDD'|'YYMM'|'YYYYMMDD';
export function formatDate(date:string, format:DateFormat){ const d=new Date(`${date}T00:00:00`); if(Number.isNaN(d.getTime())) return ''; const y=String(d.getFullYear()),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0'); return format==='YYMM'?y.slice(2)+m:format==='YYYYMMDD'?y+m+day:y.slice(2)+m+day; }
export function generateMark(input:{lotId:string;waferNumber:string;productId:string;layer:string;date:string;digits:number;dateFormat:DateFormat;separator:string;uppercase:boolean}){ const errors:string[]=[]; if(!input.lotId.trim()) errors.push('Lot ID is required.'); if(!input.waferNumber.trim()) errors.push('Wafer number is required.'); if(input.digits<1||input.digits>6) errors.push('Wafer number digits must be between 1 and 6.'); if(!/^[A-Za-z0-9]+$/.test(input.waferNumber)) errors.push('Wafer number must contain only letters and numbers.'); if(!/^[A-Za-z0-9]*$/.test(input.lotId+input.productId+input.layer)) errors.push('Lot ID, Product ID and Layer may contain only letters and numbers.'); const wafer=input.waferNumber.replace(/^0+(?=\d)/,'').padStart(input.digits,'0'); const date=formatDate(input.date,input.dateFormat); if(!date) errors.push('Date must be valid.'); const parts=[input.lotId,wafer,date,input.layer]; let mark=parts.join(input.separator); if(input.uppercase) mark=mark.toUpperCase(); return {mark,errors,characters:mark.length}; }
export function checksum(value:string, algorithm:'None'|'Luhn'|'Mod 10'|'Mod 11'|'CRC-8'='None'){ if(algorithm==='None') return ''; const digits=[...value].map(c=>c.charCodeAt(0)); const sum=digits.reduce((a,b,i)=>a+b*(i%2?2:1),0); return algorithm==='CRC-8'?String(sum%256).padStart(3,'0'):String(sum% (algorithm==='Mod 11'?11:10)); }