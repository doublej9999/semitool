export function calculateYield(gross:number, good:number, defect:number){ const errors:string[]=[]; if(!Number.isFinite(gross)||gross<=0) errors.push('Gross die must be greater than 0.'); if(!Number.isFinite(good)||good<0) errors.push('Good die cannot be negative.'); if(!Number.isFinite(defect)||defect<0) errors.push('Defect die cannot be negative.'); if(good>gross) errors.push('Good die cannot exceed gross die.'); if(defect>gross) errors.push('Defect die cannot exceed gross die.'); if(good+defect>gross) errors.push('Good and defect die cannot exceed gross die.'); if(errors.length) return {errors} as const; return {errors:[],yield:good/gross*100,rejectRate:defect/gross*100} as const; }